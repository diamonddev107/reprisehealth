{% assign rangeMin =  item["minimum_value"] %}
{% assign rangeMax =  item["maximum_value"] %}
<div class="slide-inner" data-range-min="{{ item['minimum_value'] }}">
  <div class="slide-top">
    <div class="slide-header">
      <div class="question-heading slide-question slide-question-small text-center">{{ item["question_text"] }}</div>
    </div> 
  </div>
  <div class="slide-bottom">
    {% render 'include-number-range', min: rangeMin, max: rangeMax, step: 1, active: 3, message: 'Worst it could be.', emoticon: 'semi-happy' %}
    <div class="slide-continue-wrapper">
      <p class="nothing-selected text-center">Please select an answer before continuing :)</p>
      <a class="btn--kleva btn--quiz-continue ">Continue</a>
    </div>
  </div>
</div>